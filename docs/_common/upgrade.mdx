<span>Hello {props.name}</span>

:::warning

Back up your data! Applying upgrades can lead to data loss.

:::

<p>
  This guide covers basics to consider when upgrading {props.name}, please also
  visit{' '}
  <a href={`https://github.com/ory/${props.repo}/blob/master/CHANGELOG.md`}>
    CHANGELOG.md
  </a>{' '}
  for more details.
</p>
<p>To upgrade {props.name} follow these steps:</p>
<ol>
  <li>Make a backup.</li>
  <li>
    <a href={`../install`}>Install</a> the new version (depending on how you run{' '}
    {props.name}).
  </li>
  <li>
    <b>
      Run <a href={`../cli/${props.migrate}`}>{props.repo} migrate</a>
    </b>{' '}
    to run the SQL queries.
  </li>
</ol>

<p>
  {props.name} will run the migrate command for all versions. This means
  upgrading from any version to the latest version directly is possible.{' '}
</p>
<p>
  Should you run into problems with a direct upgrade, consider a stepped upgrade
  and please open a{' '}
  <a href={`https://github.com/ory/${props.repo}/discussions`}>discussion</a> or
  visit the the community <a href="https://slack.ory.sh/">chat</a> .
</p>

### Upgrade Tips

:::info

<span>
  Visit{' '}
  <b>
    <a href={`https://github.com/ory/${props.repo}/blob/master/CHANGELOG.md`}>
      CHANGELOG.md
    </a>
  </b>{' '}
  for an overview of breaking changes for each version of {props.name}.
</span>

:::

Take the following steps to ensure no data loss:

> Please note: These are recommendations and should be used with other Ops best
> practices. The steps required for a smooth and secure upgrade process may vary
> with different setups, tech stacks, and environments.

1. Write down a high-level upgrade plan
   - Who will perform the upgrade?
   - How will the upgrade process look?
   - What components are affected?
1. Devise roll-out plan
   - When is the upgrade due?
   - Will there be an outage?
   - How long will it be?
   - What's your rollback plan?
1. Back up everything!
1. Run a trial upgrade on a local environment.
1. Run an upgrade on a staging environment.
1. Perform tests on staging & prepare production environment.
1. Run the upgrade on production.
