:::warning

<p>
  Don't save secrets such as API keys, credentials, or PII directly in{' '}
  {props.format}.
  <br />
  {props.format} used for {props.use} aren't stored in an encrypted format in
  Ory Cloud .
</p>

:::
