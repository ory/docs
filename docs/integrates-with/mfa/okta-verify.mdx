---
id: okta-verify
title: Okta Verify as a second factor
sidebar_label: Okta Verify
---

```mdx-code-block
import Admonition from '@theme/Admonition'

<Admonition type="note" title="Community-contributed integration">
Okta Verify is not natively supported as a Kratos MFA method.
Integration wires Okta Verify as an Ory Actions webhook running an
Okta Factors API check. Reference:
[ory/integrates/mfa/okta-verify](https://github.com/ory/integrates/tree/main/mfa/okta-verify).
</Admonition>
```

[Okta Verify](https://help.okta.com/en-us/content/topics/mobile/okta-verify-overview.htm)
— Okta's mobile MFA app (push, TOTP, FastPass). Useful when Okta is
the workforce identity store but Ory is the customer-facing layer.

## How it works

Ory Action on `login.after` with `can_interrupt: true`:

1. User completes primary auth in Ory.
2. Handler calls Okta's Factors API
   (`/api/v1/users/{userId}/factors/{factorId}/verify`).
3. Okta Verify push goes to the user's mobile device.
4. Handler polls Okta for the result.
5. On `SUCCESS` return success; on `REJECTED` / timeout, block.

For most use cases, native
[WebAuthn MFA](../../kratos/mfa/20_webauthn-fido-yubikey.mdx) or TOTP is
simpler. Okta Verify makes sense primarily when it's already deployed
across the workforce.

## Notable

- Requires an **Okta SSWS API token** scoped to the Okta org.
- Ory identity must be mapped to an Okta user (typically via
  `metadata_public.okta_user_id` set at enrollment).
- Push requires the user's mobile device to be online.

## Resources

- [Okta Factors API](https://developer.okta.com/docs/reference/api/factors/)
- Reference: [ory/integrates/mfa/okta-verify](https://github.com/ory/integrates/tree/main/mfa/okta-verify)
