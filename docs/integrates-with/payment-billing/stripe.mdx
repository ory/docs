---
id: stripe
title: Stripe integration
sidebar_label: Stripe
---

```mdx-code-block
import Admonition from '@theme/Admonition'

<Admonition type="note" title="Community-contributed integration">
This integration is community-maintained. Reference:
[ory/integrates/payment-billing/stripe](https://github.com/ory/integrates/tree/main/payment-billing/stripe).
</Admonition>
```

[Stripe](https://stripe.com) — payment and subscription platform.
Create Stripe Customer records at signup and consume Stripe
subscription webhooks to keep subscription state on the Ory identity.

## How it works

**Outbound** — Ory Action on `registration.after` → handler creates a
Stripe Customer with `metadata.ory_identity_id`; PATCHes
`metadata_public.stripe.customer_id` on the identity.

**Inbound** — Stripe webhooks (`customer.subscription.*`,
`invoice.payment_failed`, etc.) → handler verifies
`Stripe-Signature` over the raw body, resolves Ory identity by
`metadata.ory_identity_id`, PATCHes
`metadata_public.stripe.subscription_state`.

## Notable

- **Pin a Stripe API version** in your handler's headers — Stripe
  rolls API versions; unpinned clients break.
- Webhook signature verification must use the **raw** request body
  (before JSON parsing).
- Stripe webhooks are at-least-once and can arrive out of order — handler
  must be idempotent and tolerant of event ordering.
- Subscription gating belongs in application code, not auth flow.

## Resources

- [Stripe API documentation](https://docs.stripe.com/api)
- [Stripe webhook signatures](https://docs.stripe.com/webhooks/signature)
- [Ory Actions](/docs/actions)
- Reference: [ory/integrates/payment-billing/stripe](https://github.com/ory/integrates/tree/main/payment-billing/stripe)
