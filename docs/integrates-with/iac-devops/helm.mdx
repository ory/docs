---
id: helm
title: Deploy self-hosted Ory with Helm
sidebar_label: Helm
---

<head>
  <link rel="canonical" href="https://www.ory.com/docs/hydra/self-hosted/kubernetes-helm-chart" />
</head>

```mdx-code-block
import Admonition from '@theme/Admonition'

<Admonition type="info" title="Primary documentation">
The official Ory Helm charts are documented at
[Kubernetes Helm Chart](../../hydra/self-hosted/kubernetes-helm-chart.mdx).
That page is the source of truth; this entry exists so the charts are
discoverable alongside the other IaC and DevOps integrations.
</Admonition>
```

The official Ory Helm charts deploy the self-hosted Ory stack (Kratos,
Hydra, Keto, Oathkeeper) on Kubernetes. Charts live in
[github.com/ory/k8s](https://github.com/ory/k8s) and are hosted at
`https://k8s.ory.sh/helm/charts`.

```bash
helm repo add ory https://k8s.ory.sh/helm/charts
helm repo update
helm install kratos ory/kratos --namespace ory --create-namespace -f kratos-values.yaml
```

See [Kubernetes Helm Chart](../../hydra/self-hosted/kubernetes-helm-chart.mdx)
for the full guide, and [Terraform](./terraform.mdx) for managing Ory
**Network** (rather than self-hosted) resources as code.
