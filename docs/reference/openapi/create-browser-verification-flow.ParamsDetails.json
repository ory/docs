{
  "parameters": [
    {
      "description": "The URL to return the browser to after the flow was completed.",
      "in": "query",
      "name": "return_to",
      "schema": { "type": "string" }
    }
  ]
}
