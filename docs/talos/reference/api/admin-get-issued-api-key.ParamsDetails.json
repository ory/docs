{
  "parameters": [
    {
      "description": "UUID of the issued key, taken from the path\n(`/v2alpha1/admin/issuedApiKeys/{key_id}`). REQUIRED.",
      "in": "path",
      "name": "key_id",
      "required": true,
      "schema": { "type": "string" }
    }
  ]
}
